name: CI/CD

on: push

jobs:
  super-lint:
    name: Lint code base
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Super-Linter
        uses: github/super-linter@v4
        env:
          DEFAULT_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build:
    name: Build Qt Project
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Qt
        uses: jurplel/install-qt-action@v2
        with:
          version: '6.7.1'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_msvc2019_64'

      - name: Verify Qt Installation
        run: |
          if not exist C:\Qt\6.7.1\msvc2019_64\bin\qmake.exe (
            echo Qt installation not found!
            exit 1
          ) else (
            echo Qt installation verified.
          )

      - name: Set up environment variables
        run: |
          echo C:\Qt\6.7.1\msvc2019_64\bin >> $GITHUB_ENV
          echo C:\Qt\Tools\QtCreator\bin >> $GITHUB_ENV

      - name: Add Qt to PATH
        run: echo C:\Qt\6.7.1\msvc2019_64\bin >> $GITHUB_PATH

      - name: Verify qmake version
        run: qmake -v

      - name: Build project
        run: |
          mkdir build
          cd build
          qmake ../qmakewithsounds.pro -d  # Ensure the path to the .pro file is correct
          nmake /f Makefile.Debug VERBOSE=1

      - name: Upload build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: build-artifacts
          path: build/

  deploy:
    name: Deploy to Server
    runs-on: windows-latest
    needs: build

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Download build artifacts
        uses: actions/download-artifact@v2
        with:
          name: build-artifacts
          path: build/

